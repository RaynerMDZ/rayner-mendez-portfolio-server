# Server Configuration
server.port=8080
server.error.include-message=always
server.error.include-binding-errors=always

#spring.profiles.active=@spring.profiles.active@
spring.profiles.active=${SYSTEM_PROFILE}